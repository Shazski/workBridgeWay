import {
  findUserByEmail_repo,
  SignUpUser_repo,
  editUser_repo,
  addUserSkills_repo,
  removeUserSkills_repo,
  updateUserAbout_repo,
} from "./userRepo";

export = {
  findUserByEmail_repo,
  SignUpUser_repo,
  editUser_repo,
  addUserSkills_repo,
  removeUserSkills_repo,
  updateUserAbout_repo,
};
